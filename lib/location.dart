class Destination {
  String pid, locname, state, description, latitude, longitude, url, contact, address, imagename;
  Destination({
    this.pid,
    this.locname,
    this.state,
    this.description,
    this.latitude,
    this.longitude,
    this.url,
    this.contact,
    this.address,
    this.imagename
  });
}
